/**
 * @module choice-quit
 */

import { quitQuestion } from "../question/question-quit.mjs";

export const quitChoice = Object.freeze({
    name: "Quit",
    value: quitQuestion
});

export default quitChoice;
